spring:
  application:
    name: vains-monitor
  cloud:
    nacos:
      discovery:
        server-addr: http://abb383cdb121.c.methodot.com
      config:
        file-extension: yaml
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        # 监控客户端配置
        extension-configs:
          # 公共配置
          - data-id: vains-common-${spring.profiles.active}.yaml
            refresh: true
  profiles:
    active: prod
