spring:
  application:
    name: vains-system
  cloud:
    nacos:
      discovery:
        server-addr: http://kwqqr48rgo.cdhttp.cn/nacos
      config:
        file-extension: yaml
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        # 监控客户端配置
        extension-configs:
          # 公共配置
          - data-id: vains-common-${spring.profiles.active}.yaml
            refresh: true
  profiles:
    active: prod
