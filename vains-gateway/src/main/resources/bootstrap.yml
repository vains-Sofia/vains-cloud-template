spring:
  application:
    name: vains-gateway
  cloud:
    nacos:
      discovery:
        server-addr: http://ebf6669c0582.c.methodot.com
      config:
        file-extension: yaml
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        # 监控客户端配置
        extension-configs:
          # 公共配置
          - data-id: vains-common-${spring.profiles.active}.yaml
            refresh: true
  profiles:
    active: prod
